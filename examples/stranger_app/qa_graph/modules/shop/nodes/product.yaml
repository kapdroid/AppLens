identity:
  route: /product
  anchors: [btn_add_to_cart, lbl_product_name]
payload:
  assertions:
    - { type: widget_exists, key: btn_add_to_cart }
  edges:
    - { action: tap, key: btn_add_to_cart, target: cart }
    - { action: back, target: catalog }
  tags: [shopping]
