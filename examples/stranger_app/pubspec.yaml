name: stranger_app
description: A small demo Flutter app wired up end-to-end — AppLens's permanent zero-special-access proof.
publish_to: none
version: 1.0.0
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # AppLens Tier-0 integration: dev-dependencies only, no production code touched.
  applens_core: any
  applens_runner: any
  applens_cli: any
