# actions/applens-run

The AppLens GitHub Action — emulator boot → plan-compile → run → report upload →
PR comment with the summary. It lands in **Session 5** (the walking skeleton),
alongside the CLI. This directory is a placeholder until then; Session 0 CI is
the headless workflow in [`../../.github/workflows/ci.yaml`](../../.github/workflows/ci.yaml).
