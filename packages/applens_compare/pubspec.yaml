name: applens_compare
description: Standalone tolerant, AA-aware GoldenFileComparator for plain flutter_test goldens — usable with zero AppLens knowledge.
publish_to: none
version: 0.1.0-dev
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  image: ^4.2.0

dev_dependencies:
  flutter_lints: ^6.0.0
