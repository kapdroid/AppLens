/// AppLens crawler: budgeted breadth-first exploration that proposes a draft
/// graph (Phase 4). This is a stub until Session 10 — it ships only this
/// documented placeholder so the workspace resolves and CI is green from the
/// first commit.
library;

/// Entry point for the exploration engine. Implemented in Session 10.
Never crawl() =>
    throw UnimplementedError('applens_crawler lands in Session 10 (Phase 4).');
