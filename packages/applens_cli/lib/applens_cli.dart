/// AppLens CLI: the command-line entrypoint and composition root — validate,
/// plan, graph, report, init, run.
library;

export 'src/cli.dart';

/// The AppLens CLI version, surfaced by `applens --version`.
const String applensCliVersion = '0.1.0-dev';
