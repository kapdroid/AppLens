/// AppLens CLI: the command-line entrypoint and composition root — init,
/// validate, plan, run, report, graph. The commands land starting in Session 5;
/// this is the placeholder until then.
library;

/// The AppLens CLI version, surfaced by `applens --version`.
const String applensCliVersion = '0.1.0-dev';
