name: applens_sdk
description: Optional in-app package — TestClock, introspection service extension, seed hooks. Stub at Session 0.
publish_to: none
version: 0.1.0-dev
environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
